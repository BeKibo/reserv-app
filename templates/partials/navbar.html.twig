<nav class="bg-violet-500 p-4">
    <div class="container mx-auto flex items-center justify-between">
        <div class="flex space-x-6">
            <a href="{{ path('app_salle') }}" class="text-white hover:text-blue-400 font-semibold">Salle</a>
            <a href="{{ path('reservation_index') }}" class="text-white hover:text-blue-400 font-semibold">Réservation</a>

            {% if is_granted('ROLE_ADMIN') %}
                <a href="{{ path('admin') }}" class="text-white hover:text-red-400 font-semibold">Admin</a>
            {% endif %}
        </div>

        <div class="text-base text-white">
            {% if app.user %}
            Hello, {{ app.user.nom }} |
                <a href="{{ path('app_logout') }}" class="hover:underline text-orange-400 font-semibold text-base">Déconnexion</a>
            {% else %}
                <a href="{{ path('app_login') }}" class="hover:underline text-yellow-400 font-semibold text-base">Connexion</a>
            {% endif %}
        </div>
    </div>
</nav>
